= Module 5: Preemption Testing
:navtitle: Module 5: Preemption
:toc: macro
:toclevels: 1
:icons: font

https://kueue.sigs.k8s.io/docs/concepts/preemption[Preemption^] is the mechanism that gives reserved workloads priority over unreserved ones. When a user with a reservation submits a workload and there aren't enough free resources, Kueue evicts unreserved workloads to make room.

In this module, you'll simulate a multi-user GPU contention scenario using the workload Helm chart and observe preemption in action.

== Learning objectives

By the end of this module, you'll be able to:

* Deploy unreserved workloads that consume GPU resources
* Trigger preemption by submitting a reserved workload
* Observe the Kueue admission and eviction process
* Test preemption with both full GPUs and MIG partitions

== Kueue state after reservations

Once users have reserved GPUs through the booking app, the Kueue resource hierarchy includes per-user ClusterQueues with protected quota, and reserved LocalQueues in each namespace:

image::kueue-with-reservations.png[Kueue Hierarchy with Reservations,link=self,window=blank,width=700]

The red LocalQueues (`{user-user1}`, `{user-user2}`) point to per-user ClusterQueues with `reclaimWithinCohort: Any`. The unreserved ClusterQueue's `nominalQuota` has been reduced from 8 to 5 GPUs to account for the 3 GPUs reserved.

== How preemption works in this environment

image::preemption-flow.png[Preemption Flow,link=self,window=blank,width=700]

The preemption model is based on Kueue's Cohort borrowing rules:

----
1. Unreserved workloads run using shared Cohort quota
2. User makes a reservation → booking app creates ClusterQueue with nominalQuota
3. User submits workload → ClusterQueue has reclaimWithinCohort: Any
4. Kueue preempts unreserved workloads borrowing from the Cohort
5. Reserved workload gets resources
6. Unreserved workload re-queues, waits for resources
----

The `unreserved` ClusterQueue has no reclaim rights, so its workloads can always be preempted. User ClusterQueues have `reclaimWithinCohort: Any`, meaning they can preempt any workload borrowing from the same Cohort.

== Exercise 1: Fill the cluster with unreserved workloads

[IMPORTANT]
====
**Acting as: `{user-user1}`** +
Log in to the OpenShift CLI:

[source,console,role=execute,subs=attributes+]
----
oc login --insecure-skip-tls-verify $(oc whoami --show-server) -u {user-user1} -p {password}
----
====

First, let's consume GPU resources with unreserved workloads.

. Deploy an unreserved workload requesting 8 full GPUs (all available):
+
[source,console,role=execute,subs=attributes+]
----
helm template gpu-deploy oci://quay.io/eformat/gpu-test-workloads -n user-{user-user1} \
  --set kind=Deployment \
  --set name=gpu-fake-gpu \
  --set replicas=1 \
  --set gpuCount=8 \
  --set gpuResourceType=nvidia.com/gpu \
  --set reserved=false \
  | oc -n user-{user-user1} apply -f-
----
+
Expected output:
+
[source,text]
----
deployment.apps/gpu-fake-gpu created
----

. Wait for the deployment to be ready:
+
[source,console,role=execute,subs=attributes+]
----
oc -n user-{user-user1} rollout status deployment/gpu-fake-gpu --timeout=60s
----
+
Expected output:
+
[source,text]
----
deployment "gpu-fake-gpu" successfully rolled out
----

. Verify all GPUs are consumed:
+
[source,console,role=execute,subs=attributes+]
----
oc get clusterqueue unreserved -o jsonpath='{.status.flavorsUsage}' | jq .
----
+
Expected output:
+
[source,json]
----
{
  "borrowed": "8",
  "name": "nvidia.com/gpu",
  "total": "8"
}
----

. Verify nvidia-smi shows the GPUs:
+
[source,console,role=execute,subs=attributes+]
----
oc -n user-{user-user1} exec $(oc -n user-{user-user1} get pod -l app=gpu-fake-gpu -o name | head -1) -- nvidia-smi
----
+
You should see 8 NVIDIA GB200 NVL GPUs listed in the `nvidia-smi` output table. Each row represents one simulated GPU with its memory usage and utilization.

. You can also check the workload status in the Booking App.
+
image::unreserved-all-8.png[Unreserved Workload Running with 8 GPUs,link=self,window=blank,width=700]

=== Verify

&#10003; Unreserved deployment is running with 8 GPUs +
&#10003; ClusterQueue shows all GPU resources in use

== Exercise 2: Trigger preemption with a reserved workload

We are now going to trigger preemption with a reserved workload. We will create a reserved workload before we book a GPU for today. This gives us a chance to see the preemption happen in action.

[IMPORTANT]
====
**Acting as: `{user-user2}`** +
Log in to the OpenShift CLI:

[source,console,role=execute,subs=attributes+]
----
oc login --insecure-skip-tls-verify $(oc whoami --show-server) -u {user-user2} -p {password}
----
====

Now deploy a reserved workload from a different user. This simulates a user with a GPU reservation claiming their resources.

. Deploy a reserved workload requesting 1 GPU:
+
[source,console,role=execute,subs=attributes+]
----
helm template gpu-deploy oci://quay.io/eformat/gpu-test-workloads -n user-{user-user2} \
  --set kind=Deployment \
  --set name=gpu-fake-gpu \
  --set replicas=1 \
  --set gpuCount=1 \
  --set gpuResourceType=nvidia.com/gpu \
  --set reserved=true \
  | oc -n user-{user-user2} apply -f-
----
+
Expected output:
+
[source,text]
----
deployment.apps/gpu-fake-gpu created
----

. The workload will be in a `SchedulingGated` state as we have not yet booked a GPU for today.
+
[source,console,role=execute,subs=attributes+]
----
oc -n user-{user-user2} get pods,workload
----
+
Expected output:
+
[source,text]
----
NAME                                READY   STATUS            RESTARTS   AGE
pod/gpu-fake-gpu-74868b8b79-4cxdw   0/1     SchedulingGated   0          38s

NAME                                                              QUEUE      RESERVED IN   ADMITTED   FINISHED   AGE
workload.kueue.x-k8s.io/pod-gpu-fake-gpu-74868b8b79-4cxdw-18555   reserved                                       38s
----

[IMPORTANT]
====
**Acting as: `{user-admin}`** +
Log in to the OpenShift CLI:

[source,console,role=execute,subs=attributes+]
----
oc login --insecure-skip-tls-verify $(oc whoami --show-server) -u {user-admin} -p {password}
----
====

. Watch the preemption happen in the terminal:
+
[source,console,role=execute,subs=attributes+]
----
oc get events -A --sort-by='.lastTimestamp' --field-selector reason=Preempted -w
----

[IMPORTANT]
====
**Acting as: `{user-user2}`** +
Log in to the OpenShift Console at `{openshift_console_url}` with username `{user-user2}` and password `{password}`.
====

. Open the **GPU Booking** > **Bookings** page in the OpenShift Console and book 1 GPU for today.

. The pod will be admitted by Kueue but scheduling now needs to re-run. Delete the pod to trigger scheduling. From the OpenShift Console, navigate to **Workloads** > **Pods** and delete the pod.
+
image::delete-pod.png[Delete Pod,link=self,window=blank,width=700]

. Back on the **{user-admin}** terminal - we now expected to see the preemption event:
+
[source,text]
----
NAMESPACE    LAST SEEN   TYPE     REASON      OBJECT                                             MESSAGE
user-user1   0s          Normal   Preempted   workload/pod-gpu-fake-gpu-54959878d8-nclg6-a68ff   Preempted to accommodate a workload (UID: 79cd54c4-2146-451a-96c8-5ca1609a451e, JobUID: 7e9fa1f0-3072-4e20-9d4d-54ae8007955f) due to reclamation within the cohort; preemptor path: /unreserved/user-user2; preemptee path: /unreserved/unreserved
----
+
Press `Ctrl+C` after you see preemption events. Kueue evicts the unreserved workload to free one GPU for the reserved request.

. Check the workload status:
+
[source,console,role=execute,subs=attributes+]
----
oc get workloads -A -o custom-columns=\
NAMESPACE:.metadata.namespace,\
NAME:.metadata.name,\
QUEUE:.spec.queueName,\
ADMISSION:.status.admission.clusterQueue,\
ADMITTED:.status.conditions[0].status
----
+
Expected output - the unreserved workload is not admitted and the reserved workload is admitted:
+
[source,text]
----
NAMESPACE    NAME                                      QUEUE        ADMISSION    ADMITTED
user-user1   pod-gpu-fake-gpu-54959878d8-m4zdc-84bd2   unreserved   <none>       False
user-user2   pod-gpu-fake-gpu-74868b8b79-c8hlg-af2e1   reserved     user-user2   True
----

. Verify the reserved workload is running:
+
[source,console,role=execute,subs=attributes+]
----
oc -n user-{user-user2} rollout status deployment/gpu-fake-gpu --timeout=60s
----
+
Expected output:
+
[source,text]
----
deployment "gpu-fake-gpu" successfully rolled out
----

. Check nvidia-smi on the reserved workload:
+
[source,console,role=execute,subs=attributes+]
----
oc -n user-{user-user2} exec $(oc -n user-{user-user2} get pod -l app=gpu-fake-gpu -o name | head -1) -- nvidia-smi
----
+
You should see the nvidia-smi output for the reserved workload.

. We can also check the workload status in the Booking App.
+
image::preempted-workload-1.png[Reserved Workload Preempted Unreserved Workload,link=self,window=blank,width=700]
+
You should see the reserved workload is running (red) and the preemption event is shown in the console as well.

=== Verify

&#10003; Reserved workload preempted the unreserved workload +
&#10003; We saw the preemption event in the terminal +
&#10003; nvidia-smi shows GPU allocation in the reserved workload +
&#10003; We saw the preempted workload in the Booking App

== Exercise 3: Test MIG partition preemption

Preemption also works with MIG partitions. Let's test with MIG-1g.10gb devices. Lets quickly run through the steps to deploy a MIG workload and see the preemption in action.

[IMPORTANT]
====
**Acting as: `{user-user1}`** +
Switch to the {user-user1} CLI context:

[source,console,role=execute,subs=attributes+]
----
oc login --insecure-skip-tls-verify $(oc whoami --show-server) -u {user-user1} -p {password}
----
====

. Deploy an unreserved workload consuming 56 MIG-1g.10gb partitions:
+
[source,console,role=execute,subs=attributes+]
----
helm template gpu-deploy oci://quay.io/eformat/gpu-test-workloads -n user-{user-user1} \
  --set kind=Deployment \
  --set name=gpu-fake-10gb \
  --set replicas=1 \
  --set gpuCount=56 \
  --set gpuResourceType=nvidia.com/mig-1g.10gb \
  --set reserved=false \
  | oc -n user-{user-user1} apply -f-
----

. Wait for deployment:
+
[source,console,role=execute,subs=attributes+]
----
oc -n user-{user-user1} rollout status deployment/gpu-fake-10gb --timeout=60s
----

. Check the running pods:
+
[source,console,role=execute,subs=attributes+]
----
oc -n user-{user-user1} get pods
----
+
Expected output:
+
[source,text]
----
oc get pods
NAME                             READY   STATUS            RESTARTS   AGE
gpu-fake-10gb-5d687f5d44-gxncs   1/1     Running           0          7s
gpu-fake-gpu-54959878d8-m4zdc    0/1     SchedulingGated   0          21m
----
+
We can see the full GPU is `SchedulingGated` (it is still preempted) and the MIG workload is `Running`.

. We can also check the workload status in the Booking App.
+
image::unreserved-56.png[Unreserved MIG Workload Running with 56 MIG Partitions,link=self,window=blank,width=700]
+
You should see the **{user-user2}** reserved workload is running (red) and the **{user-user1}** unreserved MIG workload in (green).

[IMPORTANT]
====
**Acting as: `{user-user2}`** +
Log in to the OpenShift Console at `{openshift_console_url}` with username `{user-user2}` and password `{password}`.
====

. Open the **GPU Booking** > **Bookings** page in the OpenShift Console and book 8 MIG-1g.10gb partitions for today.

[IMPORTANT]
====
**Acting as: `{user-user2}`** +
Switch to the {user-user2} CLI context:

[source,console,role=execute,subs=attributes+]
----
oc login --insecure-skip-tls-verify $(oc whoami --show-server) -u {user-user2} -p {password}
----
====

. Deploy a reserved workload requesting 8 MIG partitions:
+
[source,console,role=execute,subs=attributes+]
----
helm template gpu-deploy oci://quay.io/eformat/gpu-test-workloads -n user-{user-user2} \
  --set kind=Deployment \
  --set name=gpu-fake-10gb \
  --set replicas=1 \
  --set gpuCount=8 \
  --set gpuResourceType=nvidia.com/mig-1g.10gb \
  --set reserved=true \
  | oc -n user-{user-user2} apply -f-
----

. Watch preemption and verify both are running:
+
[source,console,role=execute,subs=attributes+]
----
oc -n user-{user-user2} rollout status deployment/gpu-fake-10gb --timeout=60s
----

. Check nvidia-smi on the MIG workloads:
+
[source,console,role=execute,subs=attributes+]
----
oc -n user-{user-user1} exec $(oc -n user-{user-user1} get pod -l app=gpu-fake-10gb -o name | head -1) -- nvidia-smi
oc -n user-{user-user2} exec $(oc -n user-{user-user2} get pod -l app=gpu-fake-10gb -o name | head -1) -- nvidia-smi
----
+
Notice the MIG partition size (10 GB) shown in nvidia-smi output.

=== Verify

&#10003; Unreserved MIG workload preempted by reserved MIG workload +
&#10003; Both MIG workloads running with correct partition counts

== Exercise 4: Clean up all workloads

=== {user-user1}: Delete workloads

[IMPORTANT]
====
**Acting as: `{user-user1}`**

[source,console,role=execute,subs=attributes+]
----
oc login --insecure-skip-tls-verify $(oc whoami --show-server) -u {user-user1} -p {password}
----
====

. Remove {user-user1}'s test deployments:
+
[source,console,role=execute,subs=attributes+]
----
oc -n user-{user-user1} delete deployment gpu-fake-gpu
oc -n user-{user-user1} delete deployment gpu-fake-10gb
----

=== {user-user2}: Delete workloads

[IMPORTANT]
====
**Acting as: `{user-user2}`**

[source,console,role=execute,subs=attributes+]
----
oc login --insecure-skip-tls-verify $(oc whoami --show-server) -u {user-user2} -p {password}
----
====

. Remove {user-user2}'s test deployments:
+
[source,console,role=execute,subs=attributes+]
----
oc -n user-{user-user2} delete deployment gpu-fake-gpu
oc -n user-{user-user2} delete deployment gpu-fake-10gb
----

. Verify all GPU resources are free:
+
[source,console,role=execute,subs=attributes+]
----
oc get clusterqueues -o custom-columns=\
NAME:.metadata.name,\
ADMITTED:.status.admittedWorkloads
----
+
All ClusterQueues should show 0 admitted workloads.

=== Verify

&#10003; All test deployments deleted +
&#10003; GPU resources fully released

== Module summary

**What you accomplished:**

* Filled the cluster with unreserved GPU workloads
* Triggered preemption by submitting a reserved workload
* Observed Kueue evicting unreserved workloads to honor reserved quotas
* Tested preemption with both full GPUs and MIG partitions

**Key takeaways:**

* Reserved workloads always win over unreserved ones through Kueue's `reclaimWithinCohort: Any` policy
* Preemption is automatic -- Kueue handles eviction without manual intervention
* MIG partitions follow the same preemption model as full GPUs
* The booking app creates the Kueue resources that make preemption possible; users only interact with the calendar
* Unreserved workloads are not lost -- they re-queue and resume when resources become available

**Next:** Head to the Conclusion for a summary of what you learned and resources to continue your journey.
